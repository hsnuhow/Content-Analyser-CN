[pytest]
testpaths = tests
# 讓測試能 import 各服務的模組（pure module 直接測；db 相依用 tests/fakes.py 注入）。
pythonpath = tests crawler-service analysis-service search-extent app .
markers =
    integration: 需外部依賴（Firestore / Vertex / 網路 / 已安裝服務套件）的整合測試。
addopts = -q
